# We don't want to modify the defaults as listed below:
#scoring:
#  score: 1
#  max_score: 3 # This is automatically computed based on this file
#               # and the testcases in the group (see documentation for details).
#  aggregation: sum
#input_validator_flags: '' # (Empty String)
#output_validator_flags: '' # (Empty String)
